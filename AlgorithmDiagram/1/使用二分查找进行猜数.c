#include<stdio.h>
#include<math.h>
int main() {
	
	binary_search(5);
	
	int find = generatew_random(); 
	
} 

/*
	a[]为要找的数组
	find 为要找的数
	inpu 为自己输入的数 
*/
int binary_search(int a[], int find, int inpu) {
	
	printf("%d\n",a);
	
}

/*
	随机生成一个整数(要找的数)，并返回 
*/
int generate_random() {
	return Math.floor(Math.random() * 101);
}
